assets:
  settings.xml: kerelape/reikai-secrets#settings.xml
install: |
  pdd --file=/dev/null
merge:
  script: |
    mvn clean test verify --errors -Dstyle.color=never
release:
  script: |-
    mvn versions:set "-DnewVersion=${tag}" -Dstyle.color=never
    git commit -am "${tag}"
    mvn clean test verify install -Dinvoker.skip
    mvn clean deploy --errors --settings ../settings.xml -Dstyle.color=never